import{describe,expect,it}from"vitest";import{TARGETED_SOURCES}from"../../services/targeted-evidence/targeted-evidence-closure-service";describe("Phase 2H controlled live smoke",()=>{for(const source of TARGETED_SOURCES.filter(x=>x.decision==="ACTIVATE"))it(source.name,async()=>{const response=await fetch(source.endpoint,{headers:{"user-agent":"FlyWorkforceRadar/1.0 public-source-validation"},signal:AbortSignal.timeout(30000)});expect(response.ok).toBe(true);const body=await response.text();expect(body.length).toBeGreaterThan(500)})});
