export interface Phase1KpiObservation{signals:number;validatedSignals:number;criticalRecords:number;criticalRecordsWithEvidence:number;opportunities:number;completeCoreOpportunities:number;duplicateRecords:number;ingestedRecords:number;falsePositiveSignals:number;verifiedSignals:number;verificationMinutes:number[];opportunitiesWithBuyerRoute:number;hotEligible:number;confirmedHotEligible:number;vamoEligible:number}
export interface KpiBaseline{formula:string;numerator:number;denominator:number;result:number|null;limitations:string}
const ratio=(formula:string,numerator:number,denominator:number,limitations="Deterministic Phase 1 integration fixtures; not a production threshold"):KpiBaseline=>({formula,numerator,denominator,result:denominator?numerator/denominator:null,limitations:denominator?limitations:"BASELINE INSUFFICIENT"});
export function computePhase1Kpis(x:Phase1KpiObservation){const verificationCount=x.verificationMinutes.length,verificationTotal=x.verificationMinutes.reduce((a,b)=>a+b,0);return{validated_signal_rate:ratio("validated signals / captured signals",x.validatedSignals,x.signals),evidence_completeness:ratio("critical records with evidence / critical records",x.criticalRecordsWithEvidence,x.criticalRecords),core_opportunity_completeness:ratio("complete core opportunities / opportunities",x.completeCoreOpportunities,x.opportunities),duplicate_rate:ratio("duplicate records / ingested records",x.duplicateRecords,x.ingestedRecords),false_positive_rate:ratio("false-positive signals / verified signals",x.falsePositiveSignals,x.verifiedSignals),verification_time:{formula:"sum verification minutes / completed verifications",numerator:verificationTotal,denominator:verificationCount,result:verificationCount?verificationTotal/verificationCount:null,limitations:verificationCount?"Mean fixture verification latency; excludes queue and human availability":"BASELINE INSUFFICIENT"},buyer_route_discovery_rate:ratio("opportunities with verified buyer route / opportunities",x.opportunitiesWithBuyerRoute,x.opportunities),hot_eligibility_precision:ratio("confirmed eligible HOT outcomes / HOT-eligible outcomes",x.confirmedHotEligible,x.hotEligible,"Deterministic confirmed outcomes only; sample is too small for production precision"),vamo:{formula:"VAMO-eligible opportunities / opportunities",numerator:x.vamoEligible,denominator:x.opportunities,result:x.opportunities?x.vamoEligible/x.opportunities:null,limitations:`Count=${x.vamoEligible}; deterministic fixture baseline only`}}}
