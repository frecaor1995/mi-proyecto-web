import{EmptyState,PageHeader}from"./foundation";
export function CapabilityPage({title,description,icon,backendReady=false}:{title:string;description:string;icon:string;backendReady?:boolean}){return <div className="page-stack"><PageHeader title={title} description={description} marker={backendReady?"Backend capability available · UI integration pending":"Planned capability"}/><EmptyState icon={icon} title={`${title} is not connected yet`} description="This UI-1 route establishes the permanent product workspace. No production intelligence is loaded or simulated here."/></div>}
