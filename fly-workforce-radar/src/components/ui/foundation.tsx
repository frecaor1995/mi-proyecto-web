import type{ReactNode}from"react";import{Icon}from"./icon";
import type{Locale}from"../../i18n/locale";import{DEFAULT_LOCALE}from"../../i18n/locale";import{t}from"../../i18n/translate";
export function PageHeader({eyebrow="Workspace",title,description,marker,locale=DEFAULT_LOCALE}:{eyebrow?:string;title:string;description:string;marker?:string;locale?:Locale}){return <header className="page-header"><div><p className="overline">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div><span className="capability-marker"><span/>{marker??t(locale,"pageHeader.defaultMarker")}</span></header>}
export function EmptyState({title,description,icon="radar",locale=DEFAULT_LOCALE,children}:{title:string;description:string;icon?:string;locale?:Locale;children?:ReactNode}){const id=`empty-${title.replaceAll(" ","-").toLowerCase()}`;return <section className="empty-state" aria-labelledby={id}><div className="empty-state-icon"><Icon name={icon}/></div><p className="overline">{t(locale,"emptyState.dataIntegrationPending")}</p><h2 id={id}>{title}</h2><p>{description}</p>{children}</section>}
export function Skeleton({label,locale=DEFAULT_LOCALE}:{label?:string;locale?:Locale}){const resolved=label??t(locale,"loading.default");return <div className="skeleton-stack" role="status" aria-label={resolved}><span/><span/><span/><span className="sr-only">{resolved}</span></div>}
export function ErrorState({title,description,locale=DEFAULT_LOCALE}:{title?:string;description?:string;locale?:Locale}){return <section className="error-state" role="alert"><Icon name="warning"/><div><h2>{title??t(locale,"error.defaultTitle")}</h2><p>{description??t(locale,"error.defaultDescription")}</p></div></section>}
