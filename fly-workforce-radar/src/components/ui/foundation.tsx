import type{ReactNode}from"react";import{Icon}from"./icon";
export function PageHeader({eyebrow="Workspace",title,description,marker="Foundation ready"}:{eyebrow?:string;title:string;description:string;marker?:string}){return <header className="page-header"><div><p className="overline">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div><span className="capability-marker"><span/>{marker}</span></header>}
export function EmptyState({title,description,icon="radar",children}:{title:string;description:string;icon?:string;children?:ReactNode}){const id=`empty-${title.replaceAll(" ","-").toLowerCase()}`;return <section className="empty-state" aria-labelledby={id}><div className="empty-state-icon"><Icon name={icon}/></div><p className="overline">Data integration pending</p><h2 id={id}>{title}</h2><p>{description}</p>{children}</section>}
export function Skeleton({label="Loading content"}:{label?:string}){return <div className="skeleton-stack" role="status" aria-label={label}><span/><span/><span/><span className="sr-only">{label}</span></div>}
export function ErrorState({title="Unable to load this workspace",description="The application foundation is available, but the requested data source could not be reached."}:{title?:string;description?:string}){return <section className="error-state" role="alert"><Icon name="warning"/><div><h2>{title}</h2><p>{description}</p></div></section>}
