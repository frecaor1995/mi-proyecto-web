import{HumanVerificationQueueView}from"../../components/human-verification-desk/human-verification-desk";import{resolveServerLocale}from"../../i18n/server-locale";import{loadVerificationQueue}from"../../server/human-verification-desk/get-human-verification-desk";export default async function Page(){const[locale,result]=await Promise.all([resolveServerLocale(),loadVerificationQueue()]);return <HumanVerificationQueueView locale={locale} result={result}/>}
