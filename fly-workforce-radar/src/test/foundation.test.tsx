import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import CommandCenter from"../app/command-center/page";
describe("Radar foundation",()=>{it("identifies the product and its honest foundation state",()=>{const markup=renderToStaticMarkup(<CommandCenter/>);expect(markup).toContain("Command Center");expect(markup).toContain("Workforce intelligence");expect(markup).toContain("Product foundation operational");expect(markup).toContain("No canonical data loaded")})});
